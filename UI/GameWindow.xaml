<Window x:Class="UI.GameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="GameWindow" Height="450" Width="400"
        KeyDown="Window_KeyDown">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Canvas Grid.Column="0" Grid.Row="0" Background="Bisque" >
            <Rectangle Height="10" Stroke="Black" Width="100" Fill="Black" Canvas.Top="380" Canvas.Left="{Binding Path = 'BatPositionLeft', Mode = OneWay}" Name="paddle"/>
            <Rectangle Height="10" Stroke="Black" Width="10" Fill="Red" Canvas.Top="{Binding Path = 'BallPositionTop', Mode = OneWay}" Canvas.Left="{Binding Path = 'BallPositionLeft', Mode = OneWay}" Name="ball"/>
        </Canvas>
        
    </Grid>
</Window>
